export const metadata = {
  title: 'Admin Login — Skillz Download',
  description: 'Sign in to the Skillz Download admin panel',
};

export default function AdminLoginLayout({ children }) {
  return <>{children}</>;
}
